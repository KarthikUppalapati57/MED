| desired state | actual state | owning layer | severity | fix | test | status |
| --- | --- | --- | --- | --- | --- | --- |
| Phase 0 baseline ledger exists and records migration/test baseline for the invoice/AP module. | Phase 0 completed. Docker daemon was running. Local Supabase startup attempted clean migration application but failed at migration `009_star_schema.sql`; test baselines and core flows are recorded below. | QA | Info | Stop and wait for explicit approval before Phase 1. | `supabase start`; `npm run test:backend`; `npm run test:frontend`; `npm run test:e2e` | Phase 0 complete |
| All migrations apply cleanly from a clean/zero local Supabase state. | Defect #1 resolved under the explicit one-file exception: `supabase/migrations/009_star_schema.sql` no longer has `AS` aliases inside the `sync_fact_inventory()` `VALUES` list, and clean apply now progresses past migration 009. A new migration blocker is recorded in the pre-Phase-2 refresh section. | Data integrity | Critical | No further Phase 1 action. | `supabase start` / clean migration apply | Resolved |
| Existing backend test suite has a recorded Phase 0 regression baseline. | `npm run test:backend` passed: 9 test files passed, 1 skipped; 32 tests passed, 3 skipped. Initial sandboxed attempt failed with `spawn EPERM`, rerun with escalation produced the recorded baseline. | QA | Info | None in Phase 0. | `npm run test:backend` | Baseline recorded |
| Existing frontend test suite has a recorded Phase 0 regression baseline. | `npm run test:frontend` failed: 1 failed, 2 passed test files; 1 failed, 3 passed tests. Failing test: `tests/frontend/Invoices.test.jsx` > `Invoices Functional Testing` > `renders the invoices page and lists invoices`; expected text `Sysco` was not found. | Frontend | Medium | Future phase: investigate invoice mock/query/render contract. | `npm run test:frontend` | Baseline recorded |
| Existing e2e test suite has a recorded Phase 0 regression baseline. | `npm run test:e2e` failed: 5 failed, 2 passed. Failed tests timed out waiting for `page.waitForLoadState("networkidle")`: dashboard unauthenticated loading, invoices auth enforcement, org-management RBAC constraints, workflow login/auth elements, workflow unhandled exceptions. | QA | Medium | Future phase: investigate app network-idle behavior and environment dependencies after migration baseline is repaired. | `npm run test:e2e` | Baseline recorded |
| Phase 0 core invoice/AP user flows are identified for later tracing. | Core flows understood from code: 1. invoice intake via upload/photo/email/EDI into extraction/review; 2. invoice validation/action/approval workflow including duplicate, missing vendor/PO/receipt, reconciliation variance, and credit requests; 3. approved invoice routing into payments, storage-only, accounting export, scheduled payments, and paid/closed history. | Frontend / Backend logic / Data integrity | Info | None in Phase 0. | Code read: `src/pages/Invoices.jsx`, `src/pages/Payments.jsx`, `src/lib/invoiceAp.js`, `src/lib/apRouting.js` | Baseline recorded |


## Phase 1 - Spec Lock

### Security RLS/RBAC

| desired state | actual state | owning layer | severity | fix | test | status |
| --- | --- | --- | --- | --- | --- | --- |
| `src/lib/apiClient.js`: [INFERRED] Entity list, filter, create-many, and delete-many helpers must automatically constrain tenant-scoped tables to the active organization, brand, and location while leaving direct get, create, update, and delete operations dependent on database RLS or guarded RPCs. | Spec lock only. | security RLS/RBAC | Info | Future phase: verify helper scope and RLS coverage. | Tenant-scope API tests. | Spec locked |
| `supabase/functions/process-email-invoices/index.ts`: [SPEC-DECIDED] Email invoice ingestion is not public: it must require a trusted scheduler or shared-secret job-token boundary, reject anonymous callers, and create invoices only inside each approved inbox configuration's owning organization even when using service-role internally. | Decision locked before Phase 2. | security RLS/RBAC | High | Future phase: enforce trusted job-token boundary. | Edge-function authorization and cross-tenant ingestion tests. | Spec decided |
| `src/components/invoices/DocumentViewer.jsx`: [SPEC-DECIDED] Invoice storage paths must include `organization_id`, and signed-URL issuance must never trust a component path alone; it must verify the document's invoice is visible to the caller's tenant before signing. | Decision locked before Phase 2. | security RLS/RBAC | High | Future phase: verify tenant-owned invoice/document before signing. | Storage signed-url tenant isolation test. | Spec decided |
| `src/components/invoices/MobileReceiptCapture.jsx`: [SPEC-DECIDED] Mobile receipt capture must receive the active organization, brand, and location from its parent context like `InvoiceUploader`, with no self-sourcing or defaulting, and all returned draft data must stay within that tenant scope. | Decision locked before Phase 2. | security RLS/RBAC | High | Future phase: pass explicit tenant context from parent. | Mobile upload tenant path and RLS test. | Spec decided |

### Data Schema/Migrations/Triggers

| desired state | actual state | owning layer | severity | fix | test | status |
| --- | --- | --- | --- | --- | --- | --- |
| `supabase/migrations/026_normalize_invoice_line_items.sql`: [SPEC] Invoice line items must be normalized into organization-scoped rows where tenant users can read their own organization's line items, manager-plus users can insert or update them, and admins can delete them. | Spec lock only. | data schema/migrations/triggers | Info | Future phase. | Clean migration plus RLS matrix. | Spec locked |
| `supabase/migrations/078_unified_invoice_ap_ledger.sql`: [SPEC-DECIDED] The unified invoice AP ledger must add payment accounts and invoice AP lifecycle fields so owning-organization members can read/write by role, platform admins may read tenant AP rows for support/audit, and platform admins have no blanket write over tenant AP operational workflow state. | Decision locked before Phase 2. | data schema/migrations/triggers | Info | Future phase. | AP ledger RLS and lifecycle tests. | Spec decided |
| `supabase/migrations/080_invoice_document_intake.sql`: [SPEC-DECIDED] Invoice documents, ingestion jobs, and secure integration credentials must be organization-scoped so owning-organization members can access by role, platform admins may read tenant document/job metadata for support/audit, and only manager-plus users in the owning organization may save tenant inbox credentials. | Decision locked before Phase 2. | data schema/migrations/triggers | Info | Future phase. | Document intake RLS and credential RPC tests. | Spec decided |
| `supabase/migrations/082_three_way_reconciliation.sql`: [SPEC-DECIDED] Three-way reconciliation must store tolerance configurations, line matches, and variances by organization so owning-organization members can read/write by role, platform admins may read for support/audit, and platform admins cannot write tenant reconciliation outcomes. | Decision locked before Phase 2. | data schema/migrations/triggers | Info | Future phase. | Reconciliation RLS and RPC tests. | Spec decided |
| `supabase/migrations/083_invoice_allocations.sql`: [SPEC-DECIDED] Invoice allocations must derive GL/category splits from tenant-owned invoices and line items, store allocations under the owning organization and optional location, allow owning-organization writes by role, and allow platform-admin read-only support/audit access. | Decision locked before Phase 2. | data schema/migrations/triggers | Info | Future phase. | Allocation calculation and RLS tests. | Spec decided |
| `supabase/migrations/084_approval_workflows.sql`: [SPEC-DECIDED] Approval policies, instances, and steps must route approvals by organization-owned rules; DB/RPC role enforcement is authoritative, frontend guards are UX-only, only required-role actors in the invoice's organization may execute approval decisions, and platform admins may read but not write tenant approval workflow records. | Decision locked before Phase 2. | data schema/migrations/triggers | Info | Future phase. | Approval policy, role, and RLS tests. | Spec decided |
| `supabase/migrations/086_invoice_audit_events.sql`: [SPEC-DECIDED] Invoice audit events must record every important invoice lifecycle change under the invoice's organization, allow owning-organization inserts through guarded workflows, allow platform-admin read-only support/audit access, and deny platform-admin operational audit insertion for tenant AP. | Decision locked before Phase 2. | data schema/migrations/triggers | Info | Future phase. | Audit trigger and RLS tests. | Spec decided |
| `supabase/migrations/100_scheduled_payments.sql`: [SPEC-DECIDED] Scheduled payments and scheduled-payment invoice links must support organization-scoped payment batches so owning-organization financial actors can manage them and platform admins may read but not create, update, or execute tenant AP scheduled payments. | Decision locked before Phase 2. | data schema/migrations/triggers | Info | Future phase. | Scheduled payment batch and RLS tests. | Spec decided |
| `supabase/migrations/102_accounting_sync.sql`: [SPEC-DECIDED] Approved invoices must be queued for accounting export in organization-scoped `accounting_export_queue` rows that owning-organization actors can manage by role and platform admins may read but not modify for tenant AP operations. | Decision locked before Phase 2. | data schema/migrations/triggers | Info | Future phase. | Export queue trigger and RLS tests. | Spec decided |
| `supabase/migrations/20260624000000_invoice_schema_upgrade.sql`: [SPEC] Invoice schema upgrades must preserve payment terms, extraction tracking, archived invoice history, and line-item total enforcement for every tenant schema and ensure archived invoices remain service-controlled and tenant-isolated. | Spec lock only. | data schema/migrations/triggers | Info | Future phase. | Tenant schema upgrade and archive-trigger tests. | Spec locked |
| `supabase/migrations/20260625000021_financial_workflow_rpc_migration.sql`: [SPEC-DECIDED] Financial workflow RPCs must create, update, soft-delete, allocate, credit, and pay invoice/AP records only after asserting service role or an authorized actor in the target organization, must not allow invoice organization changes, and must not grant platform admins blanket write over tenant AP operations. | Decision locked before Phase 2. | data schema/migrations/triggers | Info | Future phase. | Financial RPC tenant isolation tests. | Spec decided |
| `supabase/migrations/20260626000007_start_invoice_extraction_workflow.sql`: [SPEC] Invoice extraction dispatch must validate the caller's access to the invoice's organization, transition only extractable invoice statuses to extracting, and enqueue `invoice-processing` with service credentials without exposing cross-tenant records. | Spec lock only. | data schema/migrations/triggers | Info | Future phase. | Extraction dispatch RPC tests. | Spec locked |
| `supabase/migrations/20260626000009_extend_invoice_processing_pg_net_timeout.sql`: [SPEC] Edge invocation and extraction dispatch must use tenant-validated invoice payloads and an extended timeout for invoice processing while preserving service-role secret handling in private runtime settings. | Spec lock only. | data schema/migrations/triggers | Info | Future phase. | pg_net invocation timeout and tenant payload tests. | Spec locked |

### Backend RPC/Edge

| desired state | actual state | owning layer | severity | fix | test | status |
| --- | --- | --- | --- | --- | --- | --- |
| `src/lib/paymentService.js`: [INFERRED] Payment helpers must route invoice payments through tenant-safe financial RPCs rather than direct table mutation, and any payment read must include the invoice's organization when available so users can only access payments for their tenant. | Spec lock only. | backend RPC/edge | Info | Future phase. | Payment service contract tests. | Spec locked |
| `src/lib/invoiceAp.js`: [SPEC] Invoice AP status, action reason, aging, and CSV export helpers must deterministically represent the invoice workflow states without widening access beyond the tenant-scoped invoice list passed into them. | Spec lock only. | backend RPC/edge | Info | Future phase. | Pure helper unit tests. | Spec locked |
| `src/lib/apRouting.js`: [SPEC] AP routing helpers must classify approved invoices into payments, storage-only, accounting-export, or paid-history-only destinations without allowing non-payments routes to enter Bill Pay. | Spec lock only. | backend RPC/edge | Info | Future phase. | AP routing unit tests. | Spec locked |
| `src/lib/accountingConfig.js`: [INFERRED] Accounting configuration must provide a stable restaurant chart of accounts and label lookup for allocation and export UI without reading or writing tenant data. | Spec lock only. | backend RPC/edge | Info | Future phase. | COA helper unit tests. | Spec locked |
| `supabase/functions/invoice-processing/index.ts`: [SPEC] Invoice processing must require authorization, verify the invoice belongs to the payload organization before service-role processing, extract and persist invoice fields and line items only on that organization-owned invoice, and mark failures on the same tenant-scoped invoice. | Spec lock only. | backend RPC/edge | Info | Future phase. | Edge-function auth, extraction, and tenant isolation tests. | Spec locked |
| `supabase/functions/create-stripe-invoice/index.ts`: [INFERRED] Platform billing invoice creation must require an authenticated platform_admin and may read or bill any target organization only for SaaS platform billing, never for tenant AP invoice payment. | Spec lock only. | backend RPC/edge | Info | Future phase. | Platform-admin authorization tests. | Spec locked |

### Frontend Components

| desired state | actual state | owning layer | severity | fix | test | status |
| --- | --- | --- | --- | --- | --- | --- |
| `src/pages/Invoices.jsx`: [SPEC] The invoice page must let authorized users in the active organization, brand, and location intake, review, validate, approve, reject, route, export, and delete only invoices visible in that tenant context. | Spec lock only. | frontend components | Info | Future phase. | Tenant-scoped invoice UI tests. | Spec locked |
| `src/pages/Payments.jsx`: [SPEC] The payments page must show only payment-routed invoices, payment accounts, scheduled payments, and payment history for the active organization, brand, and location and must record or schedule payments through guarded financial RPCs. | Spec lock only. | frontend components | Info | Future phase. | Tenant-scoped payments UI tests. | Spec locked |
| `src/pages/Accounting.jsx`: [SPEC] The accounting page must expose export, reconciliation, GL mapping, payment accounts, budgets, and close-books workflows only over the active organization's tenant-scoped invoices, payments, vendors, integrations, mappings, and accounting queues. | Spec lock only. | frontend components | Info | Future phase. | Tenant-scoped accounting UI tests. | Spec locked |
| `src/pages/PlatformInvoices.jsx`: [INFERRED] The platform billing page must be visible only to platform_admin users and may generate Stripe SaaS billing invoices for selected organizations without exposing this capability to tenant admins or ordinary users. | Spec lock only. | frontend components | Info | Future phase. | Platform billing RBAC UI tests. | Spec locked |
| `src/components/invoices/ApprovalPolicySettings.jsx`: [SPEC] Approval policy settings must let users manage only approval policies for their active organization and define amount/role rules that control whether that organization's invoices require approval. | Spec lock only. | frontend components | Info | Future phase. | Approval settings tenant UI tests. | Spec locked |
| `src/components/invoices/ApprovalWorkflowEngine.jsx`: [SPEC-DECIDED] The approval workflow component must display and request approval actions only for the current tenant's invoice and current user's required role, while DB/RPC enforcement remains authoritative and a frontend-only approval guard is a defect. | Decision locked before Phase 2. | frontend components | Info | Future phase. | Approval step RBAC UI tests. | Spec decided |
| `src/components/invoices/BillPayWidget.jsx`: [SPEC] Bill Pay must appear only for approved, scheduled, partially paid, or paid invoices routed to payments, use payment accounts in the invoice's organization, and restrict scheduling or recording payments to authorized manager-plus roles. | Spec lock only. | frontend components | Info | Future phase. | Bill Pay routing and role UI tests. | Spec locked |
| `src/components/invoices/CategorySummaryTable.jsx`: [SPEC] Category summaries must calculate, display, and split allocations only for the current tenant's invoice and GL mappings, with allocation writes handled through tenant-safe financial RPCs. | Spec lock only. | frontend components | Info | Future phase. | Allocation UI and RPC tests. | Spec locked |
| `src/components/invoices/CreditRequestDialog.jsx`: [SPEC] Credit requests must be created only against an invoice visible to the current organization and must adjust the AP ledger through the tenant-safe credit workflow. | Spec lock only. | frontend components | Info | Future phase. | Credit request tenant UI tests. | Spec locked |
| `src/components/invoices/EmailIngestionDialog.jsx`: [SPEC-DECIDED] Email ingestion settings must save IMAP credentials only for the active organization, brand, and location through a secure credential RPC restricted to manager-plus users in that organization; platform admins may not write tenant inbox credentials except through a separately authorized SaaS support flow. | Decision locked before Phase 2. | frontend components | Info | Future phase. | Email credential RBAC UI tests. | Spec decided |
| `src/components/invoices/InvoiceAuditLog.jsx`: [SPEC] The audit log component must show lifecycle events only for the selected invoice if the current user can read that invoice's organization-scoped audit events. | Spec lock only. | frontend components | Info | Future phase. | Invoice audit tenant UI tests. | Spec locked |
| `src/components/invoices/InvoiceEditor.jsx`: [SPEC] The invoice editor must let authorized users review and correct invoice header fields, totals, line items, allocations, reconciliation variances, approval workflow, and bill-pay state only for invoices in their active tenant context. | Spec lock only. | frontend components | Info | Future phase. | Invoice editor tenant UI tests. | Spec locked |
| `src/components/invoices/InvoiceUploader.jsx`: [SPEC] Invoice upload must create a tenant-scoped invoice shell, upload the document into tenant-identifiable invoice storage, and return only invoice draft data associated with the active organization before extraction starts. | Spec lock only. | frontend components | Info | Future phase. | Upload tenant path and draft tests. | Spec locked |
| `src/components/invoices/ItemMappingSelect.jsx`: [SPEC] Item mapping must suggest and select only products available to the current organization context for mapping vendor invoice line items. | Spec lock only. | frontend components | Info | Future phase. | Item mapping tenant UI tests. | Spec locked |
| `src/components/invoices/PaymentAccountsSettings.jsx`: [SPEC] Payment account settings must create, deactivate, and display payment accounts and vendor autopay settings only for the active organization. | Spec lock only. | frontend components | Info | Future phase. | Payment account tenant UI tests. | Spec locked |
| `src/components/invoices/ReconciliationVarianceTable.jsx`: [SPEC] Reconciliation variance display and resolution must be limited to variances for the active organization's invoice and must not expose other tenants' variance rows. | Spec lock only. | frontend components | Info | Future phase. | Variance tenant UI tests. | Spec locked |
| `src/components/invoices/SplitCodingDialog.jsx`: [SPEC] Split coding must validate that split amounts or percentages exactly equal the original allocation and pass only the user's intended split rows back to the tenant-safe allocation save workflow. | Spec lock only. | frontend components | Info | Future phase. | Split coding validation tests. | Spec locked |
| `src/components/invoices/ValidationDialog.jsx`: [SPEC] Invoice validation must check duplicate invoice identity, required fields, total thresholds, reconciliation variances, and paid-status detection only against invoices and variances in the current invoice's organization. | Spec lock only. | frontend components | Info | Future phase. | Validation tenant and duplicate tests. | Spec locked |

### QA

| desired state | actual state | owning layer | severity | fix | test | status |
| --- | --- | --- | --- | --- | --- | --- |
| [SPEC-DECIDED] `tests/backend`, `tests/frontend`, and `tests/e2e` are in scope for all later phases, and future QA work should extend the existing suites rather than creating parallel test suites. | Decision locked before Phase 2. | QA | Info | Future phases may extend existing tests only. | Existing backend, frontend, and e2e suites. | Spec decided |

### Phase 1 Clarifying Questions

1. `process-email-invoices` is not public and must require a trusted scheduler or shared-secret job-token boundary.
2. Invoice storage paths must include `organization_id`, and signed URLs require invoice/document tenant visibility verification before signing.
3. `MobileReceiptCapture` must receive active organization, brand, and location from its parent context like `InvoiceUploader`.
4. Platform admins may read tenant AP operational data for support/audit but have no blanket tenant AP write power; platform-admin write is limited to SaaS billing/Stripe workflows.
5. Approval enforcement is required in both layers, with DB/RPC authoritative and frontend checks treated as UX-only.

### Phase 1 Coverage Summary

[SPEC]: 21 files; [SPEC-DECIDED]: 14 files/surfaces; [INFERRED]: 5 files; [GAP]: 0 files; QA allowlist coverage: tests/backend, tests/frontend, tests/e2e added for later phases.

## Pre-Phase-2 Gated Baseline Refresh

| desired state | actual state | owning layer | severity | fix | test | status |
| --- | --- | --- | --- | --- | --- | --- |
| Phase 1 open decisions are locked before Phase 2. | All prior open Phase 1 questions were converted to `[SPEC-DECIDED]` rows covering trusted scheduler authorization for `process-email-invoices`, organization-qualified storage and signed-URL verification for `DocumentViewer`, parent-provided tenant context for `MobileReceiptCapture`, read-only tenant AP scope for platform admins, and authoritative DB/RPC approval enforcement. | QA | Info | None. | Ledger review. | Complete |
| Later phases may extend existing QA coverage for this module. | `tests/backend`, `tests/frontend`, and `tests/e2e` are now explicitly in scope for later phases, with instruction to extend existing suites rather than create parallel suites. | QA | Info | None. | Ledger review. | Complete |
| Defect #1 migration parse failure is repaired without changing inventory semantics. | Authorized edit changed only the invalid `AS` aliases inside the `sync_fact_inventory()` `VALUES` tuple in `009_star_schema.sql`; the values remain `NULL` and clean migration apply now passes migration 009. | Data integrity | Critical | None for Defect #1. | `supabase stop --no-backup`; `supabase start` | Resolved |
| All migrations apply green from a clean/zero local Supabase state after the 009 repair. | Defect #2: clean `supabase start` still fails later at `supabase/migrations/012_fix_role_constraint_and_utility_functions.sql`, statement 14, while creating `get_my_accessible_brand_ids()` because `public.get_auth_org()` does not exist (`SQLSTATE 42883`). | Data integrity | Critical | Future phase: repair migration dependency/order without editing other migrations unless authorized. | `supabase start` | Open |
| Existing backend test suite has a refreshed baseline after the 009 repair. | `npm run test:backend` passed unchanged: 9 test files passed, 1 skipped; 32 tests passed, 3 skipped. | QA | Info | None. | `npm run test:backend` | Baseline refreshed |
| Existing frontend test suite has a refreshed baseline after the 009 repair. | `npm run test:frontend` still fails unchanged: 1 failed, 2 passed test files; 1 failed, 3 passed tests. Persisting failure: `tests/frontend/Invoices.test.jsx` expects `Sysco` on the invoice page, but the text is not found. | Frontend | Medium | Future phase: investigate invoice mock/query/render contract. | `npm run test:frontend` | Baseline refreshed |
| Existing e2e test suite has a refreshed baseline after the 009 repair. | `npm run test:e2e` now fails 3 and passes 4; prior network-idle timeout symptoms resolved, while unauthenticated/RBAC expectations persist for Dashboard, Invoices, and Org Management because each expects login or unauthorized UI and receives neither. | QA | Medium | Future phase: investigate unauthenticated route/RBAC behavior and expected login/unauthorized rendering. | `npm run test:e2e` | Baseline refreshed |

## Pre-Phase-2 Read-Only Diagnosis - Defect #2 Replayability

| desired state | actual state | owning layer | severity | fix | test | status |
| --- | --- | --- | --- | --- | --- | --- |
| `public.get_auth_org()` is available before any migration, RPC, trigger, or RLS policy calls it. | Defect #2 classification: MISORDERED. `public.get_auth_org()` is defined in repo migrations, but the first migration definition is `014_security_hardening_and_rls_optimization.sql`, which sorts after `012_fix_role_constraint_and_utility_functions.sql`; 012 creates SQL functions that call `public.get_auth_org()` and fails during from-zero replay. Non-migration repair scripts also define it in `supabase/master_repair.sql` and `supabase/super_sync_final.sql`, but those are not part of normal migration replay. | Data integrity / security RLS/RBAC | Critical | Options: (A) insert an earlier helper-bootstrap migration before 012 that creates `get_auth_role()` and `get_auth_org()`; preserves existing migration file contents but retroactively inserts history before 012. (B) edit 012 to create those helpers before `can_invite_role()` and the accessible-brand/location helpers; simplest for fresh replay but edits an already-authored migration. (C) reorder/split 014 before 012; not viable as-is because 014 revokes privileges on functions created by 012. Recommendation: prefer option A if Supabase migration history policy allows adding an older-version bootstrap; otherwise option B with explicit authorization. | Static search plus prior `supabase start` failure. | Diagnosed - awaiting decision |
| `public.get_auth_role()` is available before migrations call it. | Companion Defect #2 finding: `public.get_auth_role()` is also first defined in migration `014_security_hardening_and_rls_optimization.sql` after 012; 012's `can_invite_role()`, `get_my_accessible_brand_ids()`, and `get_my_accessible_location_ids()` call it. The current run hit `get_auth_org()` first, but a partial repair that only adds `get_auth_org()` would expose `get_auth_role()` next. | Data integrity / security RLS/RBAC | Critical | Same fix options as Defect #2: bootstrap both helpers before 012 or edit 012 to define both before dependent functions. | Static dependency scan. | Diagnosed - awaiting decision |
| Definition sites for tenant-resolution helpers are ordered before consumers. | Definition order found: `is_manager_or_above()`, `is_owner_or_admin()`, and `is_admin()` in `001_initial_schema.sql`; `get_my_accessible_brand_ids()` and `get_my_accessible_location_ids()` in `012_fix_role_constraint_and_utility_functions.sql`; `get_auth_role()` and first migration `get_auth_org()` in `014_security_hardening_and_rls_optimization.sql`; `get_my_org()` and replacement `get_auth_org()` in `030_enterprise_rls_upgrade.sql`; `is_platform_admin()` in `046_storage_security.sql` and again in `047_fix_user_metadata_vulnerability.sql`; later replacement definitions in `105_performance_optimization.sql`, `118_live_schema_repair_and_security.sql`, `123_security_hardening_app_metadata.sql`, `133_harden_rls_location_access.sql`, and `20260620012000_fix_location_manager_brand_scope.sql`. | Data integrity / security RLS/RBAC | High | Use this order map when choosing a repair; do not recover helper definitions from non-migration repair scripts without reviewing semantics against later replacements. | Repo-wide search. | Recorded |
| Module tenant isolation does not rely on absent helper functions. | Blast radius in scoped invoice/AP migrations: `026_normalize_invoice_line_items.sql` RLS policies call `get_auth_org()`, `is_manager_or_above()`, and `is_admin()`; `078_unified_invoice_ap_ledger.sql` policies call `is_platform_admin()`, `is_manager_or_above()`, and `get_my_org()`; `080_invoice_document_intake.sql` policies/RPC call `is_platform_admin()`, `is_manager_or_above()`, and `get_my_org()`; `082_three_way_reconciliation.sql`, `083_invoice_allocations.sql`, `084_approval_workflows.sql`, `086_invoice_audit_events.sql`, `100_scheduled_payments.sql`, and `102_accounting_sync.sql` policies/RPCs call `is_platform_admin()` and/or `get_my_org()`; `20260625000021_financial_workflow_rpc_migration.sql` RPCs call `get_my_org()`. `20260624000000_invoice_schema_upgrade.sql`, `20260626000007_start_invoice_extraction_workflow.sql`, and `20260626000009_extend_invoice_processing_pg_net_timeout.sql` do not directly call these helpers in the searched patterns. | Security RLS/RBAC / backend RPC | High | Future audit phases must treat these helpers as core tenant-boundary dependencies; any helper repair needs RLS/RPC regression coverage for these files. | `rg` over scoped migrations and edge functions. | Recorded |
| From-zero migration replay has no additional high-confidence static blockers after Defect #2. | New Defect #3: `112_foreign_key_indexes.sql` creates indexes on `public.vendor_issues`, but `vendor_issues` is not created until `127_vendor_command_center.sql`. `CREATE INDEX IF NOT EXISTS ... ON public.vendor_issues` still fails if the table is absent, so a clean replay is expected to fail at 112 after earlier blockers are fixed. | Data integrity | High | Options: (A) move the `vendor_issues` index creation to a new migration after 127 or make it guarded by a `to_regclass('public.vendor_issues')`/dynamic SQL block; preserves existing table semantics but edits or supersedes index placement. (B) move `vendor_issues` table creation earlier before 112; broader history/order change. Recommendation: prefer a guarded/post-127 index migration; avoid moving the table unless more dependencies require it. | Static dependency scan and targeted search. | Open - awaiting decision |
| From-zero migration replay has no later numbered-vs-timestamp ordering blockers. | New Defect #4: `136_fix_invoice_status_constraint.sql` queries `public.tenant_registry`, but `tenant_registry` is created later in lexicographic migration order by `20260618153000_create_tenant_registry.sql`. A clean replay is expected to fail at 136 if it gets past 112. | Data integrity | High | Options: (A) guard the tenant-registry loop in 136 with `to_regclass('public.tenant_registry') IS NOT NULL`; preserves invoice-status semantics for public tables and skips legacy tenant schemas when registry is absent. (B) move/create `tenant_registry` before 136; larger migration-history change. Recommendation: prefer option A unless legacy tenant schema replay must be guaranteed before timestamped migrations. | Static dependency scan and targeted search. | Open - awaiting decision |
| Static analysis distinguishes create-time blockers from runtime-only risks. | Not classified as clean-apply blockers from static review: `013_version_control_rpc_functions.sql` PL/pgSQL bodies call `get_auth_role()`/`get_auth_org()` before 014, but PL/pgSQL body references are generally resolved when executed, not at function creation; `20260622210000_invoice_performance_optimization.sql` creates functions referencing `invoice_event_log` and `invoice_sync_log` before `20260622210001_enterprise_invoice_hardening.sql` creates those tables, which is also likely runtime-only unless those functions execute between the two migrations; `120_native_workflow_triggers.sql` references `subscriptions` only inside an existence guard. | Data integrity | Medium | Keep these as watch-list items for verification after the hard blockers are fixed; add tests or smoke checks if the repaired chain reaches them. | Static dependency scan. | Watch list |

## Pre-Phase-2 Replayability Repair Attempt

| desired state | actual state | owning layer | severity | fix | test | status |
| --- | --- | --- | --- | --- | --- | --- |
| Defect #2 is repaired without changing 012 semantics. | Added earlier-sorting idempotent migration `0119_bootstrap_auth_helpers_for_replay.sql` using the canonical `get_auth_role()` and `get_auth_org()` bodies from migration 014. Clean `supabase start` progressed through 012, confirming the helper-availability blocker is removed. | Data integrity / security RLS/RBAC | Critical | Keep 014 as the canonical hardening/recreate migration; bootstrap only supplies availability before 012. | `supabase stop --no-backup`; `supabase start` | Resolved |
| Replayability repair proceeds only through listed blockers unless a new blocker appears. | New Defect #5: clean replay now stops at `020_restore_public_insert_policies.sql` because it runs `DROP POLICY IF EXISTS "Anon_Insert_Demo_Requests" ON public.demo_requests`, but `public.demo_requests` does not exist yet (`SQLSTATE 42P01`). This was not one of the previously authorized Defects #2/#3/#4. | Data integrity | Critical | STOP per instruction. Likely localized guard would be required around demo_requests policy statements, but no repair is authorized in this turn. | `supabase start` | Open - awaiting authorization |
| DATA-INTEGRITY FINDING (for Phase 2): migration chain source of truth is explicit. | Migration chain was not replayable from zero; live schema appears maintained out-of-band via `master_repair.sql` / `super_sync_final.sql` and/or other repair scripts. Local ordering fixes do not resolve the source-of-truth problem. | Data integrity / QA | High | Phase 2 decision needed: choose canonical source of truth, retire or formalize repair scripts, and consider migration squash/rebaseline strategy. | Migration replay diagnostics. | Phase 2 finding - open |

## Pre-Phase-2 Bounded Batch Replayability Attempt

| desired state | actual state | owning layer | severity | fix | test | status |
| --- | --- | --- | --- | --- | --- | --- |
| Defect #5 is repaired only if it is an early-reference-to-later-created-object ordering bug. | Mandatory check failed: `public.demo_requests`, `public.access_requests`, and `public.contact_requests` are referenced by `020_restore_public_insert_policies.sql`, `113_security_hardening.sql`, `120_native_workflow_triggers.sql`, frontend pages, and edge functions, but no `CREATE TABLE` for these objects exists anywhere in the migration chain. This is not the authorized same-class ordering bug; it is a likely missing-object/schema-drift defect. | Data integrity / schema | Critical | STOP per instruction. Do not guard 020 until the source-of-truth decision is made or table definitions are recovered into migrations. | Repo-wide `rg`; clean `supabase start` failure at 020. | Open - blocked |
| Bounded batch circuit remains under limit. | Batch repaired 1 blocker (`0119_bootstrap_auth_helpers_for_replay.sql` for Defect #2) and stopped at the first non-class blocker. Defects #3 and #4 remain unreached and unmodified. | QA | Info | Await authorization for missing marketing/request table recovery or source-of-truth repair. | Git status and migration replay. | Stopped |

## Pre-Phase-2 Read-Only Drift Census

| desired state | actual state | owning layer | severity | fix | test | status |
|---|---|---|---|---|---|---|
| Migration replay should reference only schema objects created by the migration chain, or by Supabase/system schemas, at the point they are used. | MISSING-OBJECT CENSUS: 8 high-confidence table/view-like objects are referenced by migrations or module code but have no `CREATE TABLE`/`CREATE VIEW`/`CREATE MATERIALIZED VIEW` in the migration chain and no real definition in `code/supabase/master_repair.sql`, `code/supabase/super_sync_final.sql`, or other checked non-migration SQL: `access_requests`, `contact_requests`, `demo_requests`, `debug_logs`, `financial_metrics`, `labor_schedules`, `purchase_cards`, `purchase_card_transactions`. | data schema/migrations/triggers | critical | Decide source of truth: recover definitions from production/backup or intentionally remove dead references; do not mask with guards unless the object is intentionally retired. | From-zero migration replay plus schema diff against chosen baseline. | open |
| Public form workflow tables should either be defined in migrations or all references should be removed as dead code. | `access_requests` is referenced in `017_policy_consolidation_and_public_forms.sql`, `020_restore_public_insert_policies.sql`, `113_security_hardening.sql`, and `20260625000018_shared_tenancy_rls_financial_hardening.sql` catalog metadata, but no migration or repair script creates it. | data schema/migrations/triggers | critical | Recover/create the intended table in a rebaseline or retire the policies/catalog row if the feature is gone. | From-zero replay should not fail at policy DDL; public form tests should cover the decided behavior. | open |
| Public form workflow tables should either be defined in migrations or all references should be removed as dead code. | `contact_requests` is referenced in `017_policy_consolidation_and_public_forms.sql`, `020_restore_public_insert_policies.sql`, `113_security_hardening.sql`, and `20260625000018_shared_tenancy_rls_financial_hardening.sql` catalog metadata, but no migration or repair script creates it. | data schema/migrations/triggers | critical | Recover/create the intended table in a rebaseline or retire the policies/catalog row if the feature is gone. | From-zero replay should not fail at policy DDL; public form tests should cover the decided behavior. | open |
| Demo request workflow tables should either be defined in migrations or all references should be removed as dead code. | `demo_requests` is referenced in `017_policy_consolidation_and_public_forms.sql`, `020_restore_public_insert_policies.sql`, `113_security_hardening.sql`, `120_native_workflow_triggers.sql`, `20260625000018_shared_tenancy_rls_financial_hardening.sql`, `code/supabase/functions/notify-demo-request/index.ts`, and `code/supabase/functions/process-onboarding/index.ts`, but no migration or repair script creates it. | data schema/migrations/triggers / backend RPC/edge | critical | Recover/create the intended table in a rebaseline or retire the demo-request workflow end to end. | From-zero replay plus edge-function smoke test for the decided demo-request behavior. | open |
| Internal diagnostic logging should either use a defined table or avoid writing to absent schema. | `debug_logs` is written by `code/supabase/functions/invoice-processing/index.ts` and referenced by `20260625000018_shared_tenancy_rls_financial_hardening.sql`, but no migration or repair script creates it. | backend RPC/edge / data schema/migrations/triggers | high | Recover/create intended diagnostic table or replace writes with an existing logging mechanism. | Invoice-processing error-path test should not fail on missing logging table. | open |
| Admin/accounting metrics writes should target a defined metrics table. | `financial_metrics` is inserted into by `20260624000011_batch4_admin_accounting.sql`, but no migration or repair script creates it. | data schema/migrations/triggers | high | Recover/create intended metrics table or remove the insert if obsolete. | From-zero replay and admin/accounting metric query test. | open |
| Labor reporting functions should query only defined scheduling tables. | `labor_schedules` is queried by functions in `20260624000018_remote_lint_repair.sql`, but no migration or repair script creates it. | backend RPC/edge / data schema/migrations/triggers | high | Recover/create intended labor scheduling table or update the function contract after Phase 2 authorization. | Function definition replay and labor metrics RPC test. | open |
| Purchase-card indexes should attach to defined purchase-card workflow tables. | `purchase_cards` is indexed in `112_foreign_key_indexes.sql` lines 151-155, but no migration or repair script creates it. | data schema/migrations/triggers | critical | Recover/create intended purchase-card table or remove obsolete index statements in a source-of-truth repair. | From-zero replay should pass migration 112. | open |
| Purchase-card transaction indexes should attach to defined transaction workflow tables. | `purchase_card_transactions` is indexed in `112_foreign_key_indexes.sql` lines 156-162, but no migration or repair script creates it. | data schema/migrations/triggers | critical | Recover/create intended transaction table or remove obsolete index statements in a source-of-truth repair. | From-zero replay should pass migration 112. | open |
| Static drift census should distinguish true blockers from tolerated/dynamic SQL. | Not counted as missing blockers: `purchase_order_lines` appears as a comment-only reference; archived tables are created by dynamic SQL in `043_fix_archived_organizations_schema.sql`/`124_sync_archived_tables.sql`; partition tables are created in `116_table_partitioning.sql`; dashboard materialized views are created in `107_dashboard_materialized_views.sql`; `DROP POLICY IF EXISTS`/`DROP TRIGGER IF EXISTS` missing policy or trigger names are tolerated when the owning table exists. | data schema/migrations/triggers | medium | Keep this distinction in Phase 2 so repair work targets real replay failures. | Static dependency check plus from-zero replay. | noted |
| Earlier migrations should not run unguarded DDL against objects or columns created later in the chain. | ORDERING-BUG CENSUS: 6 remaining high-confidence early references plus 1 already-guarded early reference were identified. Unguarded/high-risk: `112_foreign_key_indexes.sql` indexes `vendor_issues` before `127_vendor_command_center.sql` creates it; `112_foreign_key_indexes.sql` indexes `vendors.default_payment_account_id` before `127_vendor_command_center.sql` adds it; `112_foreign_key_indexes.sql` indexes `vendor_items.last_invoice_id` before `20260622210002_price_variance_tracking.sql` adds it; `136_fix_invoice_status_constraint.sql` reads `tenant_registry` before `20260618153000_create_tenant_registry.sql` creates it; `20260622210000_invoice_performance_optimization.sql` function bodies reference `invoice_event_log` before `20260622210001_enterprise_invoice_hardening.sql` creates it; and the same file references `invoice_sync_log` before `20260622210001_enterprise_invoice_hardening.sql` creates it. Guarded/not counted as a current blocker: `120_native_workflow_triggers.sql` references `subscriptions` before `20260622210009_saas_subscriptions.sql`, but wraps the trigger DDL in an existence check. | data schema/migrations/triggers | critical | If continuing patching, add localized existence guards only after the missing-object defects are resolved; if rebaselining, encode objects in correct dependency order. | From-zero replay should pass past 112, 136, and the 20260622 invoice hardening sequence. | open |
| Repair scripts should be sufficient, internally consistent, and source-controlled if they are to serve as a rebaseline source. | `code/supabase/master_repair.sql` + `code/supabase/super_sync_final.sql` are not a complete baseline: the migration chain creates roughly 161 tables, 186 functions, 4 views, and 2 custom types, while the two repair scripts define only 5 core tables (`organizations`, `brands`, `locations`, `profiles`, `audit_logs`) and 4 helper/role functions; they omit most AP/workflow tables, all custom types/views, and the missing objects above. They also conflict semantically: canonical migration 014 and `master_repair.sql` read auth `user_metadata`, while `super_sync_final.sql` reads `app_metadata`, and both scripts include hard-coded admin promotion/recovery data mutations. | data schema/migrations/triggers | critical | Do not use these two scripts alone as the rebaseline; choose a canonical source of truth, likely prod/known-good schema plus curated migrations, then retire or quarantine repair overlays. | Apply candidate rebaseline to empty DB, diff against target schema, run RLS and workflow tests. | open |
| Magnitude should guide whether to continue localized patching or rebaseline. | MAGNITUDE SUMMARY: N=8 high-confidence missing schema objects, M=6 remaining high-confidence ordering bugs/high-risk early references, plus 1 already-guarded early reference. Repair scripts are a partial recovery overlay, not a viable complete baseline. Recommendation: prefer a formal rebaseline/source-of-truth decision over continued patching, because missing-object drift means guards would mask real schema loss and the repair scripts cover only a small subset of the migration-defined schema. | data integrity / QA | critical | Rebaseline first, then use localized patches only for verified dependency-order defects that remain after the schema source is settled. | Empty-DB replay, schema diff, and all three baseline suites after rebaseline. | open |
